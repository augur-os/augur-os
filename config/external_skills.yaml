# External skill bundles vendored as git submodules.
#
# No external bundles are active right now. Third-party skills should stay
# client-local unless a future policy review explicitly reintroduces vendoring.
#
# Per-client targets:
#   marketplace          — register vendor path as additional Claude Code marketplace
#   file_copy            — copy each skill directory wholesale
#   convert_and_copy     — run SKILL.md through the Gemini converter
#   convert_to_instructions — run SKILL.md through the Copilot .github/instructions converter

external_skill_bundles: []
