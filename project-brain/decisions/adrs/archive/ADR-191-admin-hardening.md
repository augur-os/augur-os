---
status: Implemented
date: '2026-03-02'
deciders:
- Project team
related:
- ADR-065 (dashboard hardening workflow automation)
hub: null
tags:
- admin
- hardening
superseded_by: null
---

# ADR-191: Admin Hardening

## Audit Summary

| # | Dimension | Score | Weight | Status | Key Finding |
|---|-----------|-------|--------|--------|-------------|
| 1 | UI Compliance | 55/100 | 12% | significant-gaps | No GlassCard usage in /admin/migrations |
| 2 | Page Coverage | 100/100 | 10% | good | - |
| 3 | API Completeness | 84/100 | 12% | needs-work | 2/13 API routes are stubs with no real backend logic |
| 4 | MCP Tool Wiring | 80/100 | 10% | needs-work | 2/6 pages have MCP tool calls |
| 5 | Performance | 72/100 | 10% | needs-work | No code splitting for large page: /admin |
| 6 | User Value | 75/100 | 15% | needs-work | 4 real data files found across 5/7 skills |
| 7 | Workflows | 70/100 | 8% | needs-work | 7/7 actions have working backends |
| 8 | Cross-Hub Connectivity | 40/100 | 5% | critical | No cross-hub navigation links — hub is isolated |
| 9 | Action Buttons | 71/100 | 8% | needs-work | 3/7 actions are fully-wired |
| 10 | Wow Effect | 60/100 | 10% | significant-gaps | Best candidate: Browse Files |

**Composite Score**: 72/100 (good-foundation)

## Wow Effect: Browse Files

> Open the file browser to explore the project

**Score**: 60/100

**Score breakdown**: static evidence 30/100 + runtime bonus 30 = 60/100

**Demo Flow**:
1. User clicks 'Browse Files'
2. Action executes via the configured fast-action backend
3. UI confirms completion with updated data/view state

**Expected visible output**: Open the file browser to explore the project

**Current state**: 8 candidate actions evaluated with runtime verification
**Gap to demo-ready**: Runtime partially verified — tighten UX flow and reduce action friction for a demo-ready experience

**Other candidates**:
- Open in IDE (30/100, fast_action)
- Quick Commit (30/100, llm_action)
- Open Data (30/100, fast_action)
- Score & Reorder Tabs (30/100, llm_action)

**Priority**: This is the first thing to implement in Phase 1.

## Context

Automated hardening audit of **Admin** (http://localhost:3000/admin) on 2026-03-02.
Composite score: **72/100**.

### Issues Identified

**UI Compliance** (55/100):
- No GlassCard usage in /admin/migrations
- No GlassCard usage in /admin
- No GlassCard usage in /admin/plugins

**Cross-Hub Connectivity** (40/100):
- No cross-hub navigation links — hub is isolated
- No src/lib service imports — hub doesn't consume data from other hubs
- Cross-hub data flow detected: 15 connections

**Wow Effect** (60/100):
- Best candidate: Browse Files
- Description: Open the file browser to explore the project
- Gap to demo-ready: Runtime partially verified — tighten UX flow and reduce action friction for a demo-ready experience

## Decision

Implement hardening in 2 phases, ordered by severity and user impact.

User-selected scope: **Critical + Completeness**.
Skipped dimensions: Page Coverage, API Completeness, MCP Tool Wiring, Performance, User Value, Workflows, Action Buttons.

### Phase 1: Wow Effect & Critical Gaps

**Wow Effect** (current: 60/100):
- Best candidate: Browse Files
- Description: Open the file browser to explore the project
- Gap to demo-ready: Runtime partially verified — tighten UX flow and reduce action friction for a demo-ready experience

**Cross-Hub Connectivity** (current: 40/100):
- No cross-hub navigation links — hub is isolated
- No src/lib service imports — hub doesn't consume data from other hubs
- Cross-hub data flow detected: 15 connections

### Phase 2: Completeness

**UI Compliance** (current: 55/100):
- No GlassCard usage in /admin/migrations
- No GlassCard usage in /admin
- No GlassCard usage in /admin/plugins

## Implementation Results (2026-03-02)

Post-implementation audit (`http://localhost:3001/admin`):

| Dimension | Before | After |
|---|---:|---:|
| UI Compliance | 55 | 90 |
| Cross-Hub Connectivity | 40 | 95 |
| Wow Effect | 60 | 100 |
| Composite | 72 | 86 |

Validation summary:
- `npm run build` passed in `src/dashboard/`
- Browser validation passed for `/admin`, `/admin/migrations`, `/admin/plugins`, `/admin/releases`, `/admin/renderer`, `/admin/system-cleanup` with zero page-level console errors
- MCP tool references in admin `augur.yaml` resolve to registered tool names in `plugins/admin/skills/*/augur/mcp/__init__.py`

## Consequences

### Positive

- Admin hub upgraded with standardized hardening across 3 dimensions
- Critical gaps addressed in Phase 1, enabling demo-ready wow effect
- Killer demo use case identified: Browse Files

### Negative

- Requires implementation effort across 3 dimensions
- Some dimensions may require runtime testing (performance, cross-hub connectivity)

### Neutral

- Existing working features remain untouched
- Audit report stored for trend tracking

## Alternatives Considered

This ADR was auto-generated by the dashboard hardening audit engine (ADR-065).
No manual alternatives were evaluated.

## References

- ADR-065: Dashboard hardening workflow automation (parent)
- Audit report: `admin` hub audit
- Audit timestamp: 2026-03-02T17:03:14.844618

## Implementation Prompt

> Paste this into Claude Code to execute this ADR using Agent Teams.
> Auto-generated by `generate_hardening_adr.py` from ADR-065.

You are implementing **ADR-191: Admin Hardening**.

Read the full ADR: `docs/decisions/ADR-191-admin-hardening.md`

User-selected scope: **Critical + Completeness**.
Skipped dimensions: Page Coverage, API Completeness, MCP Tool Wiring, Performance, User Value, Workflows, Action Buttons.

### Offload Protocol (ADR-054)

Before dispatching each step, check if it can be offloaded to a cheap CLI:

1. Read offload config: `cat config/system/llm.yaml` -> look for `offload:` section
2. If `offload.enabled: true` AND the step's tier is `low`:
   ```bash
   python3 plugins/orchestration/skills/executor/scripts/offload_dispatcher.py \
     --task "STEP DESCRIPTION" \
     --files "TARGET_FILE_1,TARGET_FILE_2" \
     --context-files "REFERENCE_FILE_FOR_PATTERNS" \
     --work-dir $(pwd)
   ```
3. Review the JSON output
4. Record the verdict (accept / fix / escalate)
5. If `offload.enabled: false` OR tier is `medium`/`high` -> do the step yourself

### Team Orchestration

Create a team and spawn teammates to execute the plan below:

1. **Create team**: `TeamCreate(team_name="adr-191-admin-hardening", description="Implementing ADR-191: Admin Hardening")`
2. **Create tasks**: For each step in the Execution Plan, create a task via `TaskCreate`. Set `blocked_by` for PIPELINE dependencies.
3. **Spawn teammates**: For each unique agent role, spawn a teammate:
   ```
   Task(subagent_type="general-purpose", team_name="adr-191-admin-hardening", name="{role}",
        model="{tier-model}", prompt="You are '{{role}}' on the {team_name} team.
        Read your profile: .claude/agents/{{role}}.md
        Check TaskList for your assigned tasks. After each task: TaskUpdate to complete,
        SendMessage to team lead. If blocked, move to next available task.")
   ```
4. **Profile loading**: Each teammate MUST read `.claude/agents/{name}.md` for iron laws and constraints
5. **Communication**: Teammates use `SendMessage` to report completion, request reviews, and debate
6. **Dependencies**: PARALLEL phases -> spawn all at once. PIPELINE phases -> use task blocking
7. **Shutdown**: When all phases pass, send `shutdown_request` to all teammates

**Model mapping**: `low` -> haiku, `medium` -> sonnet, `high` -> opus

### Execution Plan

**Team name**: `adr-191-admin-hardening`

#### Phase 1: Wow Effect & Critical Gaps
**Strategy**: PIPELINE

| Step | Agent | Tier | Task | Files |
|------|-------|------|------|-------|
| 1.1 | developer | high | Fix Wow Effect (60/100): Best candidate: Browse Files | `plugins/admin/skills/updater/dashboard.yaml`, `plugins/admin/skills/updater/dashboard/` |
| 1.2 | developer | medium | Fix Cross-Hub Connectivity (40/100): No cross-hub navigation links — hub is isolated | `plugins/admin/skills/updater/dashboard/` |

#### Phase 2: Completeness
**Strategy**: PIPELINE

| Step | Agent | Tier | Task | Files |
|------|-------|------|------|-------|
| 2.1 | frontend | medium | Fix UI Compliance (55/100): No GlassCard usage in /admin/migrations | `plugins/admin/skills/updater/dashboard/migrations/page.tsx`, `plugins/admin/skills/updater/dashboard/page.tsx`, `plugins/admin/skills/updater/dashboard/plugins/page.tsx` | Chains: `ui_quality_audit`, `redesign_page` |

#### Final Phase: Verification

| Step | Agent | Tier | Task |
|------|-------|------|------|
| V.1 | validator | low | Run all tests, verify no regressions |
| V.2 | frontend | low | Browser validation: open http://localhost:3000/admin in Chrome MCP, screenshot each tab, check console for runtime errors, verify auth gates render cleanly |
| V.3 | devops | low | MCP validation: cross-check all mcp_tool refs in dashboard.yaml against mcp/__init__.py registered tools |
| V.4 | architect | low | Verify ADR intent matches implementation |

### Completion Criteria

- [ ] Wow Effect improved from 60/100 to >= 90
- [ ] Cross-Hub Connectivity improved from 40/100 to >= 90
- [ ] UI Compliance improved from 55/100 to >= 90
- [ ] All phases executed
- [ ] All tests pass (`pytest tests/src/`, `npm run build`)
- [ ] Browser validation: page renders in Chrome MCP with zero console errors
- [ ] MCP validation: all tool references in dashboard.yaml resolve to registered tools
- [ ] No orphaned files or broken references
- [ ] Every skill with `dashboard/` has a `dashboard.yaml` manifest (required for mount-plugins discovery)
- [ ] No structural integrity issues (`structural_issues` in audit report is empty)
- [ ] ADR-191 status updated to Accepted
