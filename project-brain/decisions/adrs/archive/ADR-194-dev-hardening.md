---
status: Implemented
date: '2026-03-03'
deciders:
- Project team
related:
- ADR-065 (dashboard hardening workflow automation)
hub: null
tags:
- dev
- hardening
superseded_by: null
---

# ADR-194: Dev Hardening

## Audit Summary

| # | Dimension | Score | Weight | Status | Key Finding |
|---|-----------|-------|--------|--------|-------------|
| 1 | UI Compliance | 81/100 | 12% | needs-work | - |
| 2 | Page Coverage | 100/100 | 10% | good | - |
| 3 | API Completeness | 100/100 | 12% | good | - |
| 4 | MCP Tool Wiring | 90/100 | 10% | good | 12 actions have mcp_tool field (API-wrapped pattern) |
| 5 | Performance | 93/100 | 10% | good | No code splitting for large page: /dev |
| 6 | User Value | 100/100 | 15% | good | 65 real data files found across 5/5 skills |
| 7 | Workflows | 70/100 | 8% | needs-work | 27/27 actions have working backends |
| 8 | Cross-Hub Connectivity | 70/100 | 5% | needs-work | Links to 3 other hubs: /admin, /ai, /health |
| 9 | Action Buttons | 72/100 | 8% | needs-work | 12/27 actions are fully-wired |
| 10 | Wow Effect | 100/100 | 10% | good | Best candidate: Add Prompt Button Helper |

**Composite Score**: 89/100 (good-foundation)

**Scoring Confidence Note**: Action metrics use different semantics across dimensions (User Value: 15/27 autonomous, Workflows: 27/27 functional). Reconcile this classification during implementation.

## Wow Effect: Add Prompt Button Helper

> Workflow to add a new button to dashboard

**Score**: 100/100

**Score breakdown**: static evidence 40/100 + runtime bonus 70 = 100/100

**Demo Flow**:
1. User clicks 'Add Prompt Button Helper'
2. IDE chat opens with context
3. AI generates response
4. User reviews and applies

**Expected visible output**: Workflow to add a new button to dashboard

**Current state**: 28 candidate actions/workflows evaluated with runtime verification
**Gap to demo-ready**: Good static foundation — run /harden with dashboard running to verify live behavior

**Cross-hub leverage**: Pulls data from admin, ai, health

**Other candidates**:
- Analyze Patterns (40/100, llm_action)
- Improve Prompt (40/100, llm_action)
- Triage Backlog (40/100, llm_action)
- Review Changes (40/100, llm_action)

**Priority**: This is the first thing to implement in Phase 1.

## Context

Automated hardening audit of **Dev** (http://localhost:3001/dev) on 2026-03-03.
Composite score: **89/100**.

All dimensions are scoring 70 or above. This hub is in good shape
and only needs targeted polish.

## Decision

Implement hardening in 2 phases, ordered by severity and user impact.

User-selected scope: **All Phases**.

### Phase 1: Wow Effect & Critical Gaps

**Wow Effect** (current: 100/100):
- Best candidate: Add Prompt Button Helper
- Description: Workflow to add a new button to dashboard
- Gap to demo-ready: Good static foundation — run /harden with dashboard running to verify live behavior

### Phase 3: Polish & Performance

**UI Compliance** (current: 81/100):

**Workflows** (current: 70/100):
- 27/27 actions have working backends
- No chain workflows found — no automated multi-step flows

**Cross-Hub Connectivity** (current: 70/100):
- Links to 3 other hubs: /admin, /ai, /health
- No src/lib service imports — hub doesn't consume data from other hubs
- Cross-hub data flow detected: 2 connections

**Action Buttons** (current: 72/100):
- 12/27 actions are fully-wired
- 15/27 actions are frontend-only

## Consequences

### Positive

- Dev hub upgraded with standardized hardening across 4 dimensions
- Phase 1 preserves and validates the wow-effect demo flow
- Killer demo use case identified: Add Prompt Button Helper

### Negative

- Requires implementation effort across 4 dimensions
- Some dimensions may require runtime testing (performance, cross-hub connectivity)

### Neutral

- Existing working features remain untouched
- Audit report stored for trend tracking

## Alternatives Considered

This ADR was auto-generated by the dashboard hardening audit engine (ADR-065).
No manual alternatives were evaluated.

## References

- ADR-065: Dashboard hardening workflow automation (parent)
- Audit report: `dev` hub audit
- Audit timestamp: 2026-03-03T02:21:48.369954

## Implementation Validation (2026-03-03)

- Live re-audit executed against `http://localhost:3001/dev` (worktree runtime).
- Composite improved to **89/100** with **MCP Tool Wiring 90/100** and **Wow Effect 100/100**.
- Dev hub API runtime validated: `13/13` `/api/dev/*` routes returned HTTP `200`.
- Browser validation completed for `/dev`, `/dev/advisor/analytics`, `/dev/developer/tools`, `/dev/frontend/audit`, `/dev/validator/compliance` with zero console/page errors.
- MCP reference validation completed: all action `mcp_tool` references in dev skills resolve to registered tool names in skill-local `augur/mcp/__init__.py`.
- Verification gates executed: `npm run build` (dashboard) passed; `pytest tests/mcp -q` passed (`101 passed`, `12 skipped`).

## Implementation Prompt

> Paste this into Claude Code to execute this ADR using Agent Teams.
> Auto-generated by `generate_hardening_adr.py` from ADR-065.

You are implementing **ADR-194: Dev Hardening**.

Read the full ADR: `docs/decisions/ADR-194-dev-hardening.md`

User-selected scope: **All Phases**.

### Offload Protocol (ADR-054)

Before dispatching each step, check if it can be offloaded to a cheap CLI:

1. Read offload config: `cat config/system/llm.yaml` -> look for `offload:` section
2. If `offload.enabled: true` AND the step's tier is `low`:
   ```bash
   python3 plugins/orchestration/skills/executor/scripts/offload_dispatcher.py \
     --task "STEP DESCRIPTION" \
     --files "TARGET_FILE_1,TARGET_FILE_2" \
     --context-files "REFERENCE_FILE_FOR_PATTERNS" \
     --work-dir $(pwd)
   ```
3. Review the JSON output
4. Record the verdict (accept / fix / escalate)
5. If `offload.enabled: false` OR tier is `medium`/`high` -> do the step yourself

### Team Orchestration

Create a team and spawn teammates to execute the plan below:

1. **Create team**: `TeamCreate(team_name="adr-194-dev-hardening", description="Implementing ADR-194: Dev Hardening")`
2. **Create tasks**: For each step in the Execution Plan, create a task via `TaskCreate`. Set `blocked_by` for PIPELINE dependencies.
3. **Spawn teammates**: For each unique agent role, spawn a teammate:
   ```
   Task(subagent_type="general-purpose", team_name="adr-194-dev-hardening", name="{role}",
        model="{tier-model}", prompt="You are '{{role}}' on the {team_name} team.
        Read your profile: .claude/agents/{{role}}.md
        Check TaskList for your assigned tasks. After each task: TaskUpdate to complete,
        SendMessage to team lead. If blocked, move to next available task.")
   ```
4. **Profile loading**: Each teammate MUST read `.claude/agents/{name}.md` for iron laws and constraints
5. **Communication**: Teammates use `SendMessage` to report completion, request reviews, and debate
6. **Dependencies**: PARALLEL phases -> spawn all at once. PIPELINE phases -> use task blocking
7. **Shutdown**: When all phases pass, send `shutdown_request` to all teammates

**Model mapping**: `low` -> haiku, `medium` -> sonnet, `high` -> opus

### Execution Plan

**Team name**: `adr-194-dev-hardening`

#### Phase 1: Wow Effect & Critical Gaps
**Strategy**: PIPELINE

| Step | Agent | Tier | Task | Files |
|------|-------|------|------|-------|
| 1.1 | developer | high | Preserve Wow Effect (100/100) with live demo validation: Best candidate: Add Prompt Button Helper | `plugins/dev/skills/devops/augur/dashboard`, `plugins/dev/skills/advisor/augur/dashboard`, `plugins/dev/skills/developer/augur/dashboard` |

#### Phase 3: Polish & Performance
**Strategy**: PARALLEL

| Step | Agent | Tier | Task | Files |
|------|-------|------|------|-------|
| 3.1 | frontend | medium | Fix UI Compliance (81/100) (Chains: `ui_quality_audit`, `redesign_page`) | `plugins/dev/skills/devops/augur/dashboard`, `plugins/dev/skills/advisor/augur/dashboard`, `plugins/dev/skills/developer/augur/dashboard` |
| 3.2 | developer | medium | Fix Workflows (70/100): 27/27 actions have working backends | `plugins/dev/skills/devops/augur/data/actions`, `plugins/dev/skills/advisor/augur/data/actions`, `plugins/dev/skills/developer/augur/data/actions` |
| 3.3 | developer | medium | Fix Cross-Hub Connectivity (70/100): Links to 3 other hubs: /admin, /ai, /health | `plugins/dev/skills/devops/augur/dashboard`, `plugins/dev/skills/advisor/augur/dashboard`, `plugins/dev/skills/developer/augur/dashboard` |
| 3.4 | frontend | medium | Fix Action Buttons (72/100): 12/27 actions are fully-wired | `plugins/dev/skills/devops/augur.yaml`, `plugins/dev/skills/advisor/augur.yaml`, `plugins/dev/skills/developer/augur.yaml` |

#### Final Phase: Verification

| Step | Agent | Tier | Task |
|------|-------|------|------|
| V.1 | validator | low | Run all tests, verify no regressions |
| V.2 | frontend | low | Browser validation: open http://localhost:3001/dev in Chrome MCP, screenshot each tab, check console for runtime errors, verify auth gates render cleanly |
| V.3 | devops | low | MCP validation: cross-check all `mcp_tool` refs in `augur.yaml` and `augur/data/actions/*.yaml` against the current MCP tool registry/exposed server tools |
| V.4 | architect | low | Verify ADR intent matches implementation |

### Completion Criteria

- [ ] Wow Effect maintained at >= 95/100 with a verified live demo flow
- [ ] UI Compliance improved from 81/100 to >= 90
- [ ] Workflows improved from 70/100 to >= 90
- [ ] Cross-Hub Connectivity improved from 70/100 to >= 90
- [ ] Action Buttons improved from 72/100 to >= 90
- [ ] All phases executed
- [ ] All tests pass (`pytest tests/`, `npm run build`)
- [ ] Browser validation: page renders in Chrome MCP with zero console errors
- [ ] MCP validation: all tool references in `augur.yaml` and `augur/data/actions/*.yaml` resolve to registered tools
- [ ] No orphaned files or broken references
- [ ] Every skill with dashboard contributions has an `augur.yaml` manifest (required for discovery and mount)
- [ ] No structural integrity issues (`structural_issues` in audit report is empty)
- [ ] ADR-194 status updated to Accepted
