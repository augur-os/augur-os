---
status: Superseded
date: '2026-03-02'
deciders:
- Project team
related:
- ADR-065 (dashboard hardening workflow automation)
- ADR-226 (supersedes this)
hub: null
tags:
- settings
- hardening
superseded_by: null
---

# ADR-229: Settings Hardening

## Audit Summary

| # | Dimension | Score | Weight | Status | Key Finding |
|---|-----------|-------|--------|--------|-------------|
| 1 | UI Compliance | 90/100 | 12% | good | Route /settings/ai is a redirect wrapper; scoring delegat... |
| 2 | Page Coverage | 100/100 | 10% | good | - |
| 3 | API Completeness | 91/100 | 12% | good | 1/12 API routes are stubs with no real backend logic |
| 4 | MCP Tool Wiring | 92/100 | 10% | good | No explicit MCP tool references in actions/modals |
| 5 | Performance | 94/100 | 10% | good | Large page (589 lines): /settings/apis |
| 6 | User Value | 90/100 | 15% | good | 5/12 API routes have real backend logic |
| 7 | Workflows | 95/100 | 8% | good | No action manifest, but inline workflows detected via sou... |
| 8 | Cross-Hub Connectivity | 90/100 | 5% | good | Links to 1 other hubs: /ai |
| 9 | Action Buttons | 95/100 | 8% | good | No dashboard action manifest, but UI has 62 button elemen... |
| 10 | Wow Effect | 100/100 | 10% | good | Best candidate: Integrations interactive workflow |

**Composite Score**: 93/100 (production-ready)

## Wow Effect: Integrations interactive workflow

> User triggers interactive controls in IntegrationsTab.tsx and sees immediate dashboard updates

**Score**: 100/100

**Score breakdown**: static evidence 40/100 + runtime bonus 60 = 100/100

**Demo Flow**:
1. User opens Settings and triggers controls in the primary workflow surface
2. UI executes API/action-runner calls with live status feedback
3. Dashboard visibly updates to confirm successful completion

**Expected visible output**: User triggers interactive controls in IntegrationsTab.tsx and sees immediate dashboard updates

**Current state**: 8 candidate actions/workflows evaluated with runtime verification
**Gap to demo-ready**: Good static foundation — run /harden with dashboard running to verify live behavior

**Cross-hub leverage**: Pulls data from ai

**Other candidates**:
- External Services interactive workflow (30/100, inline_workflow)
- Providers interactive workflow (30/100, inline_workflow)
- page interactive workflow (30/100, inline_workflow)
- General interactive workflow (30/100, inline_workflow)

**Priority**: Treat this as the Phase 1 outcome after wiring prerequisites.

## Context

Automated hardening audit of **Settings** (http://localhost:3000/settings) on 2026-03-02.
Composite score: **93/100**.

All dimensions are scoring 70 or above. This hub is in good shape
and only needs targeted polish.

## Decision

Implement hardening in 0 phases, ordered by severity and user impact.

User-selected scope: **All Phases**.

## Implementation Results

Implemented on 2026-03-02 with the following fixes and validation:

- Fixed Settings provider runtime failures by updating [`src/dashboard/app/api/config/llm/route.ts`] to resolve `config/system/llm.yaml` first, return a safe default config when missing, and avoid 404-driven UI errors.
- Fixed stale knowledge API calls in [`src/dashboard/components/MemorySection.tsx`] from `/api/knowledge/memory/*` to mounted `/api/ai/knowledge/memory/*` endpoints.
- Added route-level code splitting for Settings APIs by moving heavy UI logic into [`src/dashboard/app/settings/apis/ApisDashboardClient.tsx`] and keeping [`src/dashboard/app/settings/apis/page.tsx`] as a lightweight dynamic wrapper.
- Re-ran hardening audit: composite **93/100** (`production-ready`), all dimensions in `good` status.
- Verification:
  - `npm run build` passed in `src/dashboard/`
  - `pytest tests/src -q` passed (`21 passed`)
  - Browser validation (Playwright) across `/settings` routes: no console errors and no visible `Failed to fetch`/`Internal Server Error`

## Consequences

### Positive

- Settings hub upgraded with standardized hardening across 0 dimensions
- Killer demo use case identified: Integrations interactive workflow

### Negative

- Requires implementation effort across 0 dimensions

### Neutral

- Existing working features remain untouched
- Audit report stored for trend tracking

## Alternatives Considered

This ADR was auto-generated by the dashboard hardening audit engine (ADR-065).
No manual alternatives were evaluated.

## References

- ADR-065: Dashboard hardening workflow automation (parent)
- Audit report: `settings` hub audit
- Audit timestamp: 2026-03-02T19:13:58.778155

## Implementation Prompt

> Paste this into Claude Code to execute this ADR using Agent Teams.
> Auto-generated by `generate_hardening_adr.py` from ADR-065.

You are implementing **ADR-229: Settings Hardening**.

Read the full ADR: `docs/decisions/ADR-229-settings-hardening.md`

User-selected scope: **All Phases**.

### Offload Protocol (ADR-054)

Before dispatching each step, check if it can be offloaded to a cheap CLI:

1. Read offload config: `cat config/system/llm.yaml` -> look for `offload:` section
2. If `offload.enabled: true` AND the step's tier is `low`:
   ```bash
   python3 plugins/orchestration/skills/executor/scripts/offload_dispatcher.py \
     --task "STEP DESCRIPTION" \
     --files "TARGET_FILE_1,TARGET_FILE_2" \
     --context-files "REFERENCE_FILE_FOR_PATTERNS" \
     --work-dir $(pwd)
   ```
3. Review the JSON output
4. Record the verdict (accept / fix / escalate)
5. If `offload.enabled: false` OR tier is `medium`/`high` -> do the step yourself

### Team Orchestration

Create a team and spawn teammates to execute the plan below:

1. **Create team**: `TeamCreate(team_name="adr-192-settings-hardening", description="Implementing ADR-229: Settings Hardening")`
2. **Create tasks**: For each step in the Execution Plan, create a task via `TaskCreate`. Set `blocked_by` for PIPELINE dependencies.
3. **Spawn teammates**: For each unique agent role, spawn a teammate:
   ```
   Task(subagent_type="general-purpose", team_name="adr-192-settings-hardening", name="{role}",
        model="{tier-model}", prompt="You are '{{role}}' on the {team_name} team.
        Read your profile: .claude/agents/{{role}}.md
        Check TaskList for your assigned tasks. After each task: TaskUpdate to complete,
        SendMessage to team lead. If blocked, move to next available task.")
   ```
4. **Profile loading**: Each teammate MUST read `.claude/agents/{name}.md` for iron laws and constraints
5. **Communication**: Teammates use `SendMessage` to report completion, request reviews, and debate
6. **Dependencies**: PARALLEL phases -> spawn all at once. PIPELINE phases -> use task blocking
7. **Shutdown**: When all phases pass, send `shutdown_request` to all teammates

**Model mapping**: `low` -> haiku, `medium` -> sonnet, `high` -> opus

### Execution Plan

**Team name**: `adr-192-settings-hardening`

#### Final Phase: Verification

| Step | Agent | Tier | Task |
|------|-------|------|------|
| V.1 | validator | low | Run all tests, verify no regressions |
| V.2 | frontend | low | Browser validation: open http://localhost:3000/settings in Chrome MCP, screenshot each tab, check console for runtime errors, verify auth gates render cleanly |
| V.3 | devops | low | MCP validation: cross-check all mcp_tool refs in manifest/source files against registered tools |
| V.4 | architect | low | Verify ADR intent matches implementation |

### Completion Criteria

- [x] All phases executed
- [x] All tests pass (`pytest tests/src/`, `npm run build`)
- [x] Browser validation: page renders in Chrome MCP with zero console errors
- [x] MCP validation: all tool references in manifests/source resolve to registered tools
- [x] No orphaned files or broken references
- [x] No structural integrity issues (`structural_issues` in audit report is empty)
- [x] ADR-229 status updated to Accepted
