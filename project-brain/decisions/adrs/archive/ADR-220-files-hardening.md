---
status: Implemented
date: '2026-03-04'
deciders:
- Project team
related:
- ADR-065 (dashboard hardening workflow automation)
hub: null
tags:
- files
- hardening
superseded_by: null
---

# ADR-220: files Hardening

## Audit Summary

| # | Dimension | Score | Weight | Status | Key Finding |
|---|-----------|-------|--------|--------|-------------|
| 1 | UI Compliance | 40/100 | 12% | critical | Missing proper layout structure in /files |
| 2 | Page Coverage | 0/100 | 10% | critical | No tabs defined |
| 3 | API Completeness | 77/100 | 12% | needs-work | 2/9 API routes are stubs with no real backend logic |
| 4 | MCP Tool Wiring | 0/100 | 10% | critical | No actions and no MCP integration — hub is entirely passive |
| 5 | Performance | 95/100 | 10% | good | Runtime probe: pages 1/1 (avg 33ms), apis 2/4 (avg 19ms) |
| 6 | User Value | 25/100 | 15% | critical | 5/9 API routes have real backend logic |
| 7 | Workflows | 0/100 | 8% | critical | No actions defined — hub has no workflows |
| 8 | Cross-Hub Connectivity | 20/100 | 5% | critical | No cross-hub navigation links — hub is isolated |
| 9 | Action Buttons | 0/100 | 8% | critical | No action buttons defined — hub has no interactivity |
| 10 | Wow Effect | 0/100 | 10% | critical | Best candidate: One-Click Folder Intelligence |

**Composite Score**: 28/100 (major-rebuild)

## Wow Effect: One-Click Folder Intelligence

> Analyze a selected folder and surface categorized insights, duplicates, and next actions in-page

**Score**: 0/100

**Score breakdown**: static evidence 0/100 + runtime bonus 0 = 0/100

**Demo Flow**:
1. Open /files and select a target folder
2. Trigger One-Click Folder Intelligence action
3. Review generated insight cards and recommended next actions

**Expected visible output**: Insight panel with top files, duplicate candidates, and actionable recommendations

**Current state**: Candidate identified but not wired as an interactive workflow
**Gap to demo-ready**: Implement action button + MCP backend + deterministic result rendering

**Cross-hub leverage**: Pulls data from ai

**Priority**: This is the first thing to implement in Phase 1.

## Context

Automated hardening audit of **files** (http://localhost:3000/files) on 2026-03-04.
Composite score: **28/100**.

### Issues Identified

**UI Compliance** (40/100):
- Missing proper layout structure in /files
- 1/1 pages lack strong typography hierarchy
- 1/1 pages miss design-system accent colors

**Page Coverage** (0/100):
- No tabs defined

**MCP Tool Wiring** (0/100):
- No actions and no MCP integration — hub is entirely passive

**User Value** (25/100):
- 5/9 API routes have real backend logic
- No pages fetch real data — all use hardcoded/mock content
- No actions defined — hub is read-only

**Workflows** (0/100):
- No actions defined — hub has no workflows

**Cross-Hub Connectivity** (20/100):
- No cross-hub navigation links — hub is isolated
- No src/lib service imports — hub doesn't consume data from other hubs
- Cross-hub data flow detected: 1 connections

**Action Buttons** (0/100):
- No action buttons defined — hub has no interactivity

**Wow Effect** (0/100):
- Best candidate: One-Click Folder Intelligence
- Description: Analyze a selected folder and surface categorized insights in-page
- UI evidence missing: candidate not yet wired in files dashboard source

## Decision

Implement hardening in 1 phase, ordered by severity and user impact.

User-selected scope: **Critical Only**.
Skipped dimensions: Cross-Hub Connectivity, UI Compliance.

### Phase 1: Wow Effect & Critical Gaps

**Wow Effect** (current: 0/100):
- Best candidate: One-Click Folder Intelligence
- Description: Analyze a selected folder and surface categorized insights in-page
- UI evidence missing: candidate not yet wired in files dashboard source

**Page Coverage** (current: 0/100):
- No tabs defined

**MCP Tool Wiring** (current: 0/100):
- No actions and no MCP integration — hub is entirely passive

**User Value** (current: 25/100):
- 5/9 API routes have real backend logic
- No pages fetch real data — all use hardcoded/mock content
- No actions defined — hub is read-only

**Workflows** (current: 0/100):
- No actions defined — hub has no workflows

**Action Buttons** (current: 0/100):
- No action buttons defined — hub has no interactivity

## User Notes

Focus this cycle on core interactive workflow hardening; defer cross-hub and visual polish.

## Consequences

### Positive

- files hub upgraded with standardized hardening across 6 dimensions
- Critical gaps addressed in Phase 1, enabling demo-ready wow effect
- Killer demo use case identified: One-Click Folder Intelligence

### Negative

- Requires implementation effort across 6 dimensions
- Some dimensions may require runtime testing (performance, cross-hub connectivity)

### Neutral

- Existing working features remain untouched
- Audit report stored for trend tracking

## Alternatives Considered

This ADR was auto-generated by the dashboard hardening audit engine (ADR-065).
No manual alternatives were evaluated.

## References

- ADR-065: Dashboard hardening workflow automation (parent)
- Audit report: `files` hub audit
- Audit timestamp: 2026-03-04T17:54:15.835314

## Implementation Prompt

> Paste this into Claude Code to execute this ADR using Agent Teams.
> Auto-generated by `generate_hardening_adr.py` from ADR-065.

You are implementing **ADR-220: files Hardening**.

Read the full ADR: `docs/decisions/ADR-220-files-hardening.md`

User-selected scope: **Critical Only**.
Skipped dimensions: Cross-Hub Connectivity, UI Compliance.

### Offload Protocol (ADR-054)

Before dispatching each step, check if it can be offloaded to a cheap CLI:

1. Read offload config: `cat config/system/llm.yaml` -> look for `offload:` section
2. If `offload.enabled: true` AND the step's tier is `low`:
   ```bash
   python3 plugins/orchestration/skills/executor/scripts/offload_dispatcher.py \
     --task "STEP DESCRIPTION" \
     --files "TARGET_FILE_1,TARGET_FILE_2" \
     --context-files "REFERENCE_FILE_FOR_PATTERNS" \
     --work-dir $(pwd)
   ```
3. Review the JSON output
4. Record the verdict (accept / fix / escalate)
5. If `offload.enabled: false` OR tier is `medium`/`high` -> do the step yourself

### Team Orchestration

Create a team and spawn teammates to execute the plan below:

1. **Create team**: `TeamCreate(team_name="adr-220-files-hardening", description="Implementing ADR-220: files Hardening")`
2. **Create tasks**: For each step in the Execution Plan, create a task via `TaskCreate`. Set `blocked_by` for PIPELINE dependencies.
3. **Spawn teammates**: For each unique agent role, spawn a teammate:
   ```
   Task(subagent_type="general-purpose", team_name="adr-220-files-hardening", name="{role}",
        model="{tier-model}", prompt="You are '{{role}}' on the {team_name} team.
        Read your profile: .claude/agents/{{role}}.md
        Check TaskList for your assigned tasks. After each task: TaskUpdate to complete,
        SendMessage to team lead. If blocked, move to next available task.")
   ```
4. **Profile loading**: Each teammate MUST read `.claude/agents/{name}.md` for iron laws and constraints
5. **Communication**: Teammates use `SendMessage` to report completion, request reviews, and debate
6. **Dependencies**: PARALLEL phases -> spawn all at once. PIPELINE phases -> use task blocking
7. **Shutdown**: When all phases pass, send `shutdown_request` to all teammates

**Model mapping**: `low` -> haiku, `medium` -> sonnet, `high` -> opus

### Execution Plan

**Team name**: `adr-220-files-hardening`

#### Phase 1: Wow Effect & Critical Gaps
**Strategy**: MIXED (lock wow-effect acceptance criteria first, then parallelize remaining critical dimensions)

| Step | Agent | Tier | Task | Files |
|------|-------|------|------|-------|
| 1.1 | developer | high | Fix Wow Effect (0/100): Best candidate: One-Click Folder Intelligence | `plugins/skills` |
| 1.2 | developer | medium | Fix Page Coverage (0/100): No tabs defined | `plugins/skills` |
| 1.3 | devops | low | Fix MCP Tool Wiring (0/100): No actions and no MCP integration — hub is entirely passive | `plugins/skills` |
| 1.4 | architect | high | Fix User Value (25/100): 5/9 API routes have real backend logic | `plugins/skills` |
| 1.5 | developer | medium | Fix Workflows (0/100): No actions defined — hub has no workflows | `plugins/skills` |
| 1.6 | frontend | medium | Fix Action Buttons (0/100): No action buttons defined — hub has no interactivity | `plugins/skills` |

#### Final Phase: Verification

| Step | Agent | Tier | Task |
|------|-------|------|------|
| V.1 | validator | low | Run all tests, verify no regressions |
| V.2 | frontend | low | Browser validation: open http://localhost:3000/files in Chrome MCP, screenshot each tab, check console for runtime errors, verify auth gates render cleanly |
| V.3 | devops | low | MCP validation: cross-check all `mcp_tool` refs in `augur.yaml` and `augur/data/actions/*.yaml` against the current MCP tool registry/exposed server tools |
| V.4 | architect | low | Verify ADR intent matches implementation |

### Completion Criteria

- [ ] Wow Effect improved from 0/100 to >= 90
- [ ] Page Coverage improved from 0/100 to >= 90
- [ ] MCP Tool Wiring improved from 0/100 to >= 90
- [ ] User Value improved from 25/100 to >= 90
- [ ] Workflows improved from 0/100 to >= 90
- [ ] Action Buttons improved from 0/100 to >= 90
- [ ] All phases executed
- [ ] All tests pass (`pytest tests/`, `npm run build`)
- [ ] Browser validation: page renders in Chrome MCP with zero console errors
- [ ] MCP validation: all tool references in `augur.yaml` and `augur/data/actions/*.yaml` resolve to registered tools
- [ ] No orphaned files or broken references
- [ ] Every skill with dashboard contributions has an `augur.yaml` manifest (required for discovery and mount)
- [ ] No structural integrity issues (`structural_issues` in audit report is empty)
- [ ] ADR-220 status updated to Accepted
