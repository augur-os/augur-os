---
status: Superseded
date: '2026-03-03'
deciders:
- Project team
related:
- ADR-065 (dashboard hardening workflow automation)
- ADR-205 (supersedes this)
hub: null
tags:
- observability
- hardening
superseded_by: null
---

# ADR-210: Observability Hardening

## Audit Summary

| # | Dimension | Score | Weight | Status | Key Finding |
|---|-----------|-------|--------|--------|-------------|
| 1 | UI Compliance | 98/100 | 12% | good | - |
| 2 | Page Coverage | 100/100 | 10% | good | - |
| 3 | API Completeness | 100/100 | 12% | good | - |
| 4 | MCP Tool Wiring | 100/100 | 10% | good | 8 actions use API-wrapped MCP pattern |
| 5 | Performance | 91/100 | 10% | good | Runtime probe: pages 4/4 (avg 544ms), apis 6/6 (avg 23ms) |
| 6 | User Value | 98/100 | 15% | good | 10 real data files found across 2/3 skills |
| 7 | Workflows | 90/100 | 8% | good | 8/8 actions have working backends |
| 8 | Cross-Hub Connectivity | 95/100 | 5% | good | Links to 4 other hubs: /admin, /ai, /health, /professional |
| 9 | Action Buttons | 100/100 | 8% | good | 8/8 actions are fully-wired |
| 10 | Wow Effect | 100/100 | 10% | good | Best candidate: Dismiss Old Notifications |

**Composite Score**: 97/100 (production-ready)

## Wow Effect: Dismiss Old Notifications

> Dismiss all read notifications older than 7 days from the feed

**Score**: 100/100

**Score breakdown**: static evidence 40/100 + runtime bonus 60 = 100/100

**Demo Flow**:
1. User clicks 'Dismiss Old Notifications'
2. Action executes via the configured fast-action backend
3. UI confirms completion with updated data/view state

**Expected visible output**: Dismiss all read notifications older than 7 days from the feed

**Current state**: 9 candidate actions/workflows evaluated with runtime verification
**Gap to demo-ready**: Runtime verified — polish the demo narrative and visible output for stakeholder walkthroughs

**Cross-hub leverage**: Pulls data from health

**Other candidates**:
- Loop History (40/100, llm_action)
- Loop Status (40/100, llm_action)
- Promote Category (40/100, llm_action)
- Run Loop Cycle (40/100, llm_action)

**Priority**: This is the first thing to implement in Phase 1.

## Context

Automated hardening audit of **Observability** (http://localhost:3006/observability?tab=sessions) on 2026-03-03.
Composite score: **97/100**.

All dimensions are scoring 70 or above. This hub is in good shape
and only needs targeted polish.

## Decision

Implement hardening in 1 phase, ordered by severity and user impact.

User-selected scope: **All Phases**.

### Phase 1: Wow Effect & Critical Gaps

**Wow Effect** (current: 100/100):
- Best candidate: Dismiss Old Notifications
- Description: Dismiss all read notifications older than 7 days from the feed
- UI evidence: surfaced in 1 hub source files

## Consequences

### Positive

- Observability hub upgraded with standardized hardening across 1 dimensions
- Phase 1 preserves and validates the wow-effect demo flow
- Killer demo use case identified: Dismiss Old Notifications

### Negative

- Requires implementation effort across 1 dimensions
- Some dimensions may require runtime testing (performance, cross-hub connectivity)

### Neutral

- Existing working features remain untouched
- Audit report stored for trend tracking

## Alternatives Considered

This ADR was auto-generated by the dashboard hardening audit engine (ADR-065).
No manual alternatives were evaluated.

## References

- ADR-065: Dashboard hardening workflow automation (parent)
- Audit report: `observability` hub audit
- Audit timestamp: 2026-03-03T21:46:08.540150

## Implementation Prompt

> Paste this into Claude Code to execute this ADR using Agent Teams.
> Auto-generated by `generate_hardening_adr.py` from ADR-065.

You are implementing **ADR-210: Observability Hardening**.

Read the full ADR: `docs/decisions/ADR-210-observability-hardening.md`

User-selected scope: **All Phases**.

### Offload Protocol (ADR-054)

Before dispatching each step, check if it can be offloaded to a cheap CLI:

1. Read offload config: `cat config/system/llm.yaml` -> look for `offload:` section
2. If `offload.enabled: true` AND the step's tier is `low`:
   ```bash
   python3 plugins/orchestration/skills/executor/scripts/offload_dispatcher.py \
     --task "STEP DESCRIPTION" \
     --files "TARGET_FILE_1,TARGET_FILE_2" \
     --context-files "REFERENCE_FILE_FOR_PATTERNS" \
     --work-dir $(pwd)
   ```
3. Review the JSON output
4. Record the verdict (accept / fix / escalate)
5. If `offload.enabled: false` OR tier is `medium`/`high` -> do the step yourself

### Team Orchestration

Create a team and spawn teammates to execute the plan below:

1. **Create team**: `TeamCreate(team_name="adr-210-observability-hardening", description="Implementing ADR-210: Observability Hardening")`
2. **Create tasks**: For each step in the Execution Plan, create a task via `TaskCreate`. Set `blocked_by` for PIPELINE dependencies.
3. **Spawn teammates**: For each unique agent role, spawn a teammate:
   ```
   Task(subagent_type="general-purpose", team_name="adr-210-observability-hardening", name="{role}",
        model="{tier-model}", prompt="You are '{{role}}' on the {team_name} team.
        Read your profile: .claude/agents/{{role}}.md
        Check TaskList for your assigned tasks. After each task: TaskUpdate to complete,
        SendMessage to team lead. If blocked, move to next available task.")
   ```
4. **Profile loading**: Each teammate MUST read `.claude/agents/{name}.md` for iron laws and constraints
5. **Communication**: Teammates use `SendMessage` to report completion, request reviews, and debate
6. **Dependencies**: PARALLEL phases -> spawn all at once. PIPELINE phases -> use task blocking
7. **Shutdown**: When all phases pass, send `shutdown_request` to all teammates

**Model mapping**: `low` -> haiku, `medium` -> sonnet, `high` -> opus

### Execution Plan

**Team name**: `adr-210-observability-hardening`

#### Phase 1: Wow Effect & Critical Gaps
**Strategy**: PIPELINE

| Step | Agent | Tier | Task | Files |
|------|-------|------|------|-------|
| 1.1 | developer | high | Preserve Wow Effect (100/100) with live demo validation: Best candidate: Dismiss Old Notifications | `plugins/observability/skills/daemon/augur/dashboard`, `plugins/observability/skills/observe/augur/dashboard`, `plugins/observability/skills/daemon/augur.yaml` |

#### Final Phase: Verification

| Step | Agent | Tier | Task |
|------|-------|------|------|
| V.1 | validator | low | Run all tests, verify no regressions |
| V.2 | frontend | low | Browser validation: open http://localhost:3006/observability?tab=sessions in Chrome MCP, screenshot each tab, check console for runtime errors, verify auth gates render cleanly |
| V.3 | devops | low | MCP validation: cross-check all `mcp_tool` refs in `augur.yaml` and `augur/data/actions/*.yaml` against the current MCP tool registry/exposed server tools |
| V.4 | architect | low | Verify ADR intent matches implementation |

### Completion Criteria

- [ ] Wow Effect maintained at >= 95/100 with a verified live demo flow
- [ ] Wow candidate is confirmed in hub UI source (action label/id binding), not manifest-only
- [ ] Wow demo includes before/after screenshots showing visible output
- [ ] All phases executed
- [ ] All tests pass (`pytest tests/`, `npm run build`)
- [ ] Browser validation: page renders in Chrome MCP with zero console errors
- [ ] MCP validation: all tool references in `augur.yaml` and `augur/data/actions/*.yaml` resolve to registered tools
- [ ] No orphaned files or broken references
- [ ] Every skill with dashboard contributions has an `augur.yaml` manifest (required for discovery and mount)
- [ ] No structural integrity issues (`structural_issues` in audit report is empty)
- [ ] ADR-210 status updated to Accepted
