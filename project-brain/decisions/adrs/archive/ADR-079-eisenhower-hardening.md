---
status: Implemented
date: '2026-02-12'
deciders:
- Project team
related:
- ADR-065 (dashboard hardening workflow automation)
hub: null
tags:
- eisenhower
- matrix
- hardening
superseded_by: null
---

# ADR-079: Eisenhower Matrix Hardening

## Audit Summary

| # | Dimension | Score | Weight | Status | Key Finding |
|---|-----------|-------|--------|--------|-------------|
| 1 | Page Coverage | 100/100 | 10% | good | - |
| 2 | API Completeness | 10/100 | 12% | critical | No API directory but 1 actions need API backends |
| 3 | MCP Tool Wiring | 15/100 | 10% | critical | Actions defined but none use MCP tools — limited autonomy |
| 4 | Performance | 29/100 | 10% | critical | Score capped at 60/100 — runtime telemetry needed for ful... |
| 5 | User Value | 0/100 | 15% | critical | No data directory — hub produces no persisted data |
| 6 | Workflows | 40/100 | 8% | critical | 1/2 actions have working backends |
| 7 | Cross-Hub Connectivity | 0/100 | 5% | critical | No cross-hub navigation links — hub is isolated |
| 8 | Wow Effect | 20/100 | 10% | critical | Best candidate: AI Prioritize |

**Composite Score**: 31/100 (major-rebuild)

## Wow Effect: AI Prioritize

> Let AI suggest task quadrants

**Score**: 20/100

**Demo Flow**:
1. User clicks 'AI Prioritize'
2. IDE chat opens with context
3. AI generates response
4. User reviews and applies

**Current state**: 2 candidate actions evaluated
**Gap to demo-ready**: Backend exists — needs live browser verification and end-to-end testing

**Other candidates**:
- Add Task (0/100, modal_workflow)

**Priority**: This is the first thing to implement in Phase 1.

## Context

Automated hardening audit of **Eisenhower Matrix** (http://localhost:3000/eisenhower) on 2026-02-12.
Composite score: **31/100**.

### Issues Identified

**API Completeness** (10/100):
- No API directory but 1 actions need API backends

**MCP Tool Wiring** (15/100):
- Actions defined but none use MCP tools — limited autonomy

**Performance** (29/100):
- Score capped at 60/100 — runtime telemetry needed for full evaluation

**User Value** (0/100):
- No data directory — hub produces no persisted data
- No API routes — hub cannot process data autonomously
- No pages fetch real data — all use hardcoded/mock content

**Workflows** (40/100):
- 1/2 actions have working backends
- 1/2 actions are YAML-only with no working backend
- Modal 'add-task' has no submitTool — form submits to nothing

**Cross-Hub Connectivity** (0/100):
- No cross-hub navigation links — hub is isolated
- No src/lib service imports — hub doesn't consume data from other hubs
- No cross-hub data flow — hub operates in a silo

**Wow Effect** (20/100):
- Best candidate: AI Prioritize
- Description: Let AI suggest task quadrants
- Gap to demo-ready: Backend exists — needs live browser verification and end-to-end testing

## Decision

Implement hardening in three phases, ordered by severity and user impact.

### Phase 1: Wow Effect & Critical Gaps

**Wow Effect** (current: 20/100):
- Best candidate: AI Prioritize
- Description: Let AI suggest task quadrants
- Gap to demo-ready: Backend exists — needs live browser verification and end-to-end testing

**API Completeness** (current: 10/100):
- No API directory but 1 actions need API backends

**MCP Tool Wiring** (current: 15/100):
- Actions defined but none use MCP tools — limited autonomy

**Performance** (current: 29/100):
- Score capped at 60/100 — runtime telemetry needed for full evaluation

**User Value** (current: 0/100):
- No data directory — hub produces no persisted data
- No API routes — hub cannot process data autonomously
- No pages fetch real data — all use hardcoded/mock content

**Workflows** (current: 40/100):
- 1/2 actions have working backends
- 1/2 actions are YAML-only with no working backend
- Modal 'add-task' has no submitTool — form submits to nothing

**Cross-Hub Connectivity** (current: 0/100):
- No cross-hub navigation links — hub is isolated
- No src/lib service imports — hub doesn't consume data from other hubs
- No cross-hub data flow — hub operates in a silo

## Consequences

### Positive

- Eisenhower Matrix hub upgraded with standardized hardening across 7 dimensions
- Critical gaps addressed in Phase 1, enabling demo-ready wow effect
- Killer demo use case identified: AI Prioritize

### Negative

- Requires implementation effort across 7 dimensions
- Some dimensions may require runtime testing (performance, cross-hub connectivity)

### Neutral

- Existing working features remain untouched
- Audit report stored for trend tracking

## Alternatives Considered

This ADR was auto-generated by the dashboard hardening audit engine (ADR-065).
No manual alternatives were evaluated.

## References

- ADR-065: Dashboard hardening workflow automation (parent)
- Audit report: `eisenhower` hub audit
- Audit timestamp: 2026-02-12T00:31:46.615172

## Implementation Prompt

> Paste this into Claude Code to execute this ADR using Agent Teams.
> Auto-generated by `generate_hardening_adr.py` from ADR-065.

You are implementing **ADR-079: Eisenhower Matrix Hardening**.

Read the full ADR: `docs/decisions/ADR-079-eisenhower-hardening.md`

### Offload Protocol (ADR-054)

Before dispatching each step, check if it can be offloaded to a cheap CLI:

1. Read offload config: `cat config/system/llm.yaml` -> look for `offload:` section
2. If `offload.enabled: true` AND the step's tier is `low`:
   ```bash
   python3 plugins/orchestration/skills/executor/scripts/offload_dispatcher.py \
     --task "STEP DESCRIPTION" \
     --files "TARGET_FILE_1,TARGET_FILE_2" \
     --context-files "REFERENCE_FILE_FOR_PATTERNS" \
     --work-dir $(pwd)
   ```
3. Review the JSON output
4. Record the verdict (accept / fix / escalate)
5. If `offload.enabled: false` OR tier is `medium`/`high` -> do the step yourself

### Team Orchestration

Create a team and spawn teammates to execute the plan below:

1. **Create team**: `TeamCreate(team_name="adr-079-eisenhower-hardening", description="Implementing ADR-079: Eisenhower Matrix Hardening")`
2. **Create tasks**: For each step in the Execution Plan, create a task via `TaskCreate`. Set `blocked_by` for PIPELINE dependencies.
3. **Spawn teammates**: For each unique agent role, spawn a teammate:
   ```
   Task(subagent_type="general-purpose", team_name="adr-079-eisenhower-hardening", name="{role}",
        model="{tier-model}", prompt="You are '{{role}}' on the {team_name} team.
        Read your profile: .claude/agents/{{role}}.md
        Check TaskList for your assigned tasks. After each task: TaskUpdate to complete,
        SendMessage to team lead. If blocked, move to next available task.")
   ```
4. **Profile loading**: Each teammate MUST read `.claude/agents/{name}.md` for iron laws and constraints
5. **Communication**: Teammates use `SendMessage` to report completion, request reviews, and debate
6. **Dependencies**: PARALLEL phases -> spawn all at once. PIPELINE phases -> use task blocking
7. **Shutdown**: When all phases pass, send `shutdown_request` to all teammates

**Model mapping**: `low` -> haiku, `medium` -> sonnet, `high` -> opus

### Execution Plan

**Team name**: `adr-079-eisenhower-hardening`

#### Phase 1: Wow Effect & Critical Gaps
**Strategy**: PIPELINE

| Step | Agent | Tier | Task | Files |
|------|-------|------|------|-------|
| 1.1 | developer | high | Fix Wow Effect (20/100): Best candidate: AI Prioritize | `plugins/productivity/skills/eisenhower/dashboard.yaml`, `plugins/productivity/skills/eisenhower/dashboard/` |
| 1.2 | developer | medium | Fix API Completeness (10/100): No API directory but 1 actions need API backends | `src/dashboard/app/api/eisenhower/`, `src/dashboard/lib/services/` |
| 1.3 | devops | low | Fix MCP Tool Wiring (15/100): Actions defined but none use MCP tools — limited autonomy | `plugins/productivity/skills/eisenhower/dashboard.yaml`, `config/dashboard/mcp_tools.yaml` |
| 1.4 | frontend | medium | Fix Performance (29/100): Score capped at 60/100 — runtime telemetry needed for ful... | `plugins/productivity/skills/eisenhower/dashboard//page.tsx` |
| 1.5 | architect | high | Fix User Value (0/100): No data directory — hub produces no persisted data | `plugins/productivity/skills/eisenhower/dashboard.yaml` |
| 1.6 | developer | medium | Fix Workflows (40/100): 1/2 actions have working backends | `plugins/productivity/skills/eisenhower/dashboard.yaml` | Chains: `generate_delight` |
| 1.7 | developer | medium | Fix Cross-Hub Connectivity (0/100): No cross-hub navigation links — hub is isolated | `plugins/productivity/skills/eisenhower/dashboard/` |

#### Final Phase: Verification

| Step | Agent | Tier | Task |
|------|-------|------|------|
| V.1 | validator | low | Run all tests, verify no regressions |
| V.2 | frontend | low | Browser validation: open http://localhost:3000/eisenhower in Chrome MCP, screenshot each tab, check console for runtime errors, verify auth gates render cleanly |
| V.3 | devops | low | MCP validation: cross-check all mcp_tool refs in dashboard.yaml against mcp/__init__.py registered tools |
| V.4 | architect | low | Verify ADR intent matches implementation |

### Completion Criteria

- [ ] Wow Effect improved from 20/100 to >= 90
- [ ] API Completeness improved from 10/100 to >= 90
- [ ] MCP Tool Wiring improved from 15/100 to >= 90
- [ ] Performance improved from 29/100 to >= 90
- [ ] User Value improved from 0/100 to >= 90
- [ ] Workflows improved from 40/100 to >= 90
- [ ] Cross-Hub Connectivity improved from 0/100 to >= 90
- [ ] All phases executed
- [ ] All tests pass (`pytest tests/src/`, `npm run build`)
- [ ] Browser validation: page renders in Chrome MCP with zero console errors
- [ ] MCP validation: all tool references in dashboard.yaml resolve to registered tools
- [ ] No orphaned files or broken references
- [ ] ADR-079 status updated to Accepted

## User Notes

This is a core daily-driver hub. Prioritize UX polish and real data integration. UI Compliance and Action Buttons dimensions were excluded from this hardening plan by user choice — UI Compliance (60/100) and Action Buttons (50/100) will be addressed in a future pass or will naturally improve as the critical dimensions are fixed.
