---
description: Enforce the shared/private vault skill-root migration contract
visibility: auto
---

# auto-skill-root-migration

Run the final skill-root migration guard so hardening fails on repo-root `skills/` directories or runtime code that resolves skills through repo-root paths.
