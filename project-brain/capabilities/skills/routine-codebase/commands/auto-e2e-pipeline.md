---
description: Validate the vault-to-dashboard data pipeline and pinpoint the failing stage
context: current
agent: general-purpose
visibility: ops
---

# /auto-e2e-pipeline

Run the end-to-end pipeline validator from vault data through RAG, MCP, API, and dashboard rendering.

## Implementation

`skills/routine-codebase/scripts/e2e_pipeline.py`
