---
description: Nightly UI and UX quality audit with accessibility, interaction, and responsive checks
context: current
agent: general-purpose
visibility: ops
---

# /auto-ui-quality

Run the UI quality audit across dashboard pages, score the problems, and apply
safe fixes when the configured difficulty allows it.

## Implementation

`skills/routine-codebase/scripts/ui_quality.py`
