# Unified page-surface actions for the audio-ingest skill (ADR-807).
# Folded from the retired augur/actions/audio-ingest-overview.yaml (Fork 4).
actions:
  - id: audio-ingest-overview
    label: Audio Ingest Overview
    icon: AudioLines
    kind: mcp
    mcp_tool: get-skill-health
    dispatch: fire
    surfaces: [page]
