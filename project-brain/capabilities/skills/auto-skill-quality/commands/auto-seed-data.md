# /auto-seed-data

Run the absorbed seed-data hardening loop under `auto-skill-quality`.

Purpose:
- find skills with packaged seed data
- report empty seed directories
- seed empty skill data directories conservatively

Implementation:
- `scripts/seed_data_ops.py`

Usage:
- `/routines run hardening`
