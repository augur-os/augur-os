# Unified page-surface actions for the plugin-pack skill (ADR-807).
# Migrated from SKILL.md x-augur-config.contributions.actions.
actions:
  - id: plugin-pack-overview
    label: Plugin Pack Overview
    icon: Package
    kind: mcp
    mcp_tool: get-skill-health
    dispatch: fire
    surfaces: [page]
