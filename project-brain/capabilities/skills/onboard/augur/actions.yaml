# Unified page-surface actions for the onboard skill (ADR-807).
# Ported from the preserved /tmp/onboard-action.md.keep (Plan 1).
actions:
  - id: onboard-status
    label: Onboard Status
    icon: Activity
    kind: mcp
    mcp_tool: get-system-health
    dispatch: ide
    surfaces: [page]
