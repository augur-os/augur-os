Augur is a local-first second brain that runs on your laptop and is operated by your AI clients. The same skills, prompts, vault, and MCP tools work across Claude Code, Codex CLI, Gemini CLI, Cursor, Copilot, and any other client that can read this repo. This file is the long form of the repo doc map: it carries the same pointer index as `llms.txt` plus the full bodies of the load-bearing agent-topic files (`agent-rules.md`, `architecture-overview.md`, `what-is-augur.md`) inlined below, so a one-shot AI client can ground itself without resolving any further paths. For per-client constitution rules see the client-specific files at the repo root (`CLAUDE.md`, `CODEX.md`, `AGENTS.md`, etc.); for the concise pointer-only version see `llms.txt`. Full agent rules and architecture follow below.
