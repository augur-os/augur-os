# Markdown Knowledge Graph

Portable deterministic graph extraction workflows for Markdown collections.
This bundle covers typed relationships from links, frontmatter, citations, and
local note structure, producing file-first graph evidence without requiring a
platform-specific store.

Platform-specific graph caches, index rebuild wiring, MCP tools, dashboard
pages, and generated-client behavior belong in an adapter outside this standard
source.
